WHITESPACE = _{ " " | "\t" }

commands = _{ (message)+ }

message = _{ WHITESPACE* ~ (uci | debug | isready) ~ NEWLINE* }

uci = @{ "uci" }

switch = { "on" | "off" }
debug = { "debug" ~ switch }

isready = @{ "isready" }

alpha = { 'a'..'z' | 'A'..'Z' }
digit = { '0'..'9' }